<UserControl x:Class="WpfApp1.Modules.CylinderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Border BorderThickness="2" CornerRadius="8" Margin="10,10,10,0" Padding="15">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="SteelBlue"/>
                <Setter Property="Background" Value="#F0F8FF"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                        <Setter Property="Background" Value="#E0E0E0"/>
                        <Setter Property="BorderBrush" Value="#A9A9A9"/>
                        <Setter Property="Opacity" Value="0.7"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <StackPanel>
            <DockPanel LastChildFill="False" Margin="0,0,0,10">
                <TextBlock Text="CYLINDER UNIT" FontWeight="Bold" Foreground="SteelBlue" VerticalAlignment="Center"/>

                <CheckBox IsChecked="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}" 
                          Content="Enabled" 
                          DockPanel.Dock="Right" 
                          VerticalAlignment="Center"/>
            </DockPanel>

            <StackPanel Orientation="Horizontal" IsEnabled="{Binding IsActive}">
                <Label Content="Operating Pressure (PSI):" VerticalAlignment="Center"/>
                <TextBox Text="{Binding PSI, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>