<UserControl x:Class="WpfApp1.Modules.SawControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300">
    <Border BorderThickness="2" CornerRadius="8" Margin="10" Padding="15">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="Crimson"/>
                <Setter Property="Background" Value="#FFF8F8"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                        <Setter Property="Background" Value="#E0E0E0"/>
                        <Setter Property="BorderBrush" Value="#A9A9A9"/>
                        <Setter Property="Opacity" Value="0.7"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <StackPanel>
            <DockPanel LastChildFill="False" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Text="SAW MODULE -" FontWeight="Bold" Foreground="Crimson" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ModuleId}" FontWeight="Bold" Foreground="Gray" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <CheckBox IsChecked="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}" 
                          Content="Enabled" 
                          DockPanel.Dock="Right" 
                          VerticalAlignment="Center"
                          FontWeight="SemiBold"/>
            </DockPanel>

            <UniformGrid Columns="2" IsEnabled="{Binding IsActive}">
                <Label Content="Blade Speed:"/>
                <TextBox Text="{Binding BladeSpeed, UpdateSourceTrigger=PropertyChanged}" 
                         VerticalContentAlignment="Center" Margin="2"/>

                <Label Content="Saw Angle:"/>
                <TextBox Text="{Binding SawAngle, UpdateSourceTrigger=PropertyChanged}" 
                         VerticalContentAlignment="Center" Margin="2"/>
            </UniformGrid>
        </StackPanel>
    </Border>
</UserControl>